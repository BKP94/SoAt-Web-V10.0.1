# .NET
**/bin/
**/obj/
**/*.user
**/*.suo
.vs/

# Environment
.env
.env.local
appsettings.Development.json

# Oracle source reference (legacy, local-only — used to build PG stub functions)
legacy_ora_src/

# Claude Code — local-only (permissions ฯลฯ); launch.json แชร์ได้
.claude/settings.local.json

# Publish output (build artifact — regenerate ด้วย dotnet publish)
publish/
**/publish/

# OS
.DS_Store
Thumbs.db

# SQL migrations (apply เข้า PG จริง แต่ไม่ track — PG = source of truth)
sql/

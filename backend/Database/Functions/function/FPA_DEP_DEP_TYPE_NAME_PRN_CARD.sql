-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;





CREATE OR REPLACE FUNCTION fpa_dep_dep_type_name_prn_card (as_type_code char) RETURNS varchar AS $body$
DECLARE

  ls_return varchar(1000);

BEGIN
  select  deposit_item_description into STRICT ls_return
  from    sc_dep_m_ucf_dep_item_type
  where (deposit_item_type = as_type_code);

return   trim(both ls_return);
exception when others then CALL spa_dberror('FPA_DEP_DEP_TYPE_NAME_PRN_CARD');return null;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION fpa_dep_dep_type_name_prn_card (as_type_code char) FROM PUBLIC;


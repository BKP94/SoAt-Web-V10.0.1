-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;





CREATE OR REPLACE FUNCTION fpb_spec_coll_owner (as_reqno char,as_coll_ref text) RETURNS varchar AS $body$
DECLARE

  ls_rc varchar(1000);

--  cursor cur_data is
--  select sc_lon_req_coll_law_owner.jungwat
--  from sc_lon_req_coll_law_owner
--  where loan_requestment_no = as_reqno
--  and coll_ref = as_coll_ref
--  order by seq_no;
--  type tabi_data is table of cur_data%rowtype index by pls_integer ;ltabi_data tabi_data;
--  li_count pls_integer;
BEGIN
--  open cur_data ;fetch cur_data bulk collect into ltabi_data ; close cur_data;
--  li_count := ltabi_data.count ;
--  for i in 1..li_count loop
--    if i > 1 then
--      ls_rc := ls_rc||',';
--    end if;
--    if is_number(trim(ltabi_data(i).jungwat)) = '1' then
--      ls_rc := ls_rc||pka_com_function.fp_get_province_name(trim(ltabi_data(i).jungwat));
--    else
--      ls_rc := ls_rc||trim(ltabi_data(i).jungwat);
--    end if;
--  end loop;
  return  ls_rc;
exception when others then return null;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION fpb_spec_coll_owner (as_reqno char,as_coll_ref text) FROM PUBLIC;


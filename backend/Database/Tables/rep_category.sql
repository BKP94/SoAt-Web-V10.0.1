CREATE TABLE rep_category (
	cat_id char(10),
	cat_name varchar(50),
	userobject varchar(40),
	ttips varchar(50),
	etips varchar(50),
	cat_comment varchar(100),
	application varchar(32),
	invisible_status char(1),
	order_by double precision
) ;



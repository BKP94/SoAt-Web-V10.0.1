-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;

DROP TRIGGER IF EXISTS tbiu_sc_fin_m_bank ON sc_fin_m_bank CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tbiu_sc_fin_m_bank() RETURNS trigger AS $BODY$
BEGIN

  if coalesce(NEW.BALANCE_BEGIN::text, '') = ''   then NEW.BALANCE_BEGIN := 0;end if;
  if coalesce(NEW.SORT_ORDER::text, '') = ''   then NEW.SORT_ORDER    := 0;end if;
  if coalesce(NEW.CHEQUE_RUNNING::text, '') = ''   then NEW.CHEQUE_RUNNING:= 0;end if;
  if coalesce(NEW.BANK_BALANCE::text, '') = ''   then NEW.BANK_BALANCE  := 0;end if;
  if coalesce(trim(both NEW.recvpay_able)::text, '') = '' then NEW.recvpay_able := '0';end if;
  if coalesce(trim(both NEW.pay_bookbank_able )::text, '') = '' then NEW.pay_bookbank_able :='0';end if;
  if coalesce(trim(both NEW.keep_monthly_able)::text, '') = '' then NEW.keep_monthly_able :='0';end if;
  if coalesce(NEW.bill_status::text, '') = ''   then NEW.bill_status  := 0;end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tbiu_sc_fin_m_bank() FROM PUBLIC;

CREATE TRIGGER tbiu_sc_fin_m_bank
	BEFORE INSERT OR UPDATE ON sc_fin_m_bank FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tbiu_sc_fin_m_bank();


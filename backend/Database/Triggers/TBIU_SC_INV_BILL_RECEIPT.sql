-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;

DROP TRIGGER IF EXISTS tbiu_sc_inv_bill_receipt ON sc_inv_bill_receipt CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tbiu_sc_inv_bill_receipt() RETURNS trigger AS $BODY$
BEGIN

  if coalesce(NEW.entry_date::text, '') = '' then NEW.entry_date := statement_timestamp();end if;
  if coalesce(NEW.entry_id::text, '') = '' then NEW.entry_id := pka_com_function.fp_login_id;end if;
  if coalesce(NEW.branch_id::text, '') = '' then NEW.branch_id := pka_com_function.fp_login_br;end if;
  if coalesce(NEW.client_id::text, '') = '' then NEW.client_id := pka_com_function.fp_login_pc;end if;
  if coalesce(NEW.receipt_status::text, '') = '' then NEW.receipt_status := '0';end if;
  if coalesce(NEW.cancle_status::text, '') = '' then NEW.cancle_status := '0';end if;
  if coalesce(NEW.fin_status::text, '') = '' then NEW.fin_status := '0';end if;

    if coalesce(NEW.principal::text, '') = '' then NEW.principal := 0;end if;
    if coalesce(NEW.interest_received::text, '') = '' then NEW.interest_received := 0;end if;
    if coalesce(NEW.tax::text, '') = '' then NEW.tax := 0;end if;
    if coalesce(NEW.fee_bank::text, '') = '' then NEW.fee_bank := 0;end if;
    if coalesce(NEW.fee_int::text, '') = '' then NEW.fee_int := 0;end if;
    if coalesce(NEW.sale_price::text, '') = '' then NEW.sale_price := 0;end if;


RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tbiu_sc_inv_bill_receipt() FROM PUBLIC;

CREATE TRIGGER tbiu_sc_inv_bill_receipt
	BEFORE INSERT OR UPDATE ON sc_inv_bill_receipt FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tbiu_sc_inv_bill_receipt();


-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;

DROP TRIGGER IF EXISTS tbiu_sc_mem_m_app_own_info ON sc_mem_m_app_own_info CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tbiu_sc_mem_m_app_own_info() RETURNS trigger AS $BODY$
BEGIN

  if coalesce(NEW.application_form_no::text, '') = '' then
    CALL spa_dberrcol('sc_mem_m_app_own_info','application_form_no',NEW.application_form_no);
  end if;

--  if pka.auto_entry then 
--    if :new.other_saving is null then
--      spa_dberrcol('sc_mem_m_app_own_info','other_saving',:new.other_saving);
--    end if;
--  end if ;
  if coalesce(NEW.own_work_age::text, '') = '' then NEW.own_work_age := 0;end if;

  if coalesce(NEW.own_member_age::text, '') = '' then NEW.own_member_age := 0;end if;

  if coalesce(NEW.own_member_month::text, '') = '' then  NEW.own_member_month := 0;end if;

  if coalesce(NEW.own_total_loan::text, '') = '' then NEW.own_total_loan := 0;end if;

  if coalesce(NEW.other_saving::text, '') = '' then
    NEW.other_saving := pka_srv_dbattrib.getdefault('sc_mem_m_app_own_info','other_saving');
  end if;

  if coalesce(NEW.share_stock::text, '') = '' then
    NEW.share_stock := 0;
  end if;

  if coalesce(NEW.share_period::text, '') = '' then
    NEW.share_period := 0;
  end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tbiu_sc_mem_m_app_own_info() FROM PUBLIC;

CREATE TRIGGER tbiu_sc_mem_m_app_own_info
	BEFORE INSERT OR UPDATE ON sc_mem_m_app_own_info FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tbiu_sc_mem_m_app_own_info();


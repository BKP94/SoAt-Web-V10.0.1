-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;

DROP TRIGGER IF EXISTS tbiu_sc_mem_m_app_share ON sc_mem_m_app_share CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tbiu_sc_mem_m_app_share() RETURNS trigger AS $BODY$
BEGIN

  if coalesce(NEW.application_form_no::text, '') = '' then
    CALL spa_dberrcol('sc_mem_m_app_share','application_form_no',NEW.application_form_no);
  end if;

  if coalesce(NEW.share_type_code::text, '') = '' then
    NEW.share_type_code := pka_srv_dbattrib.getdefault('sc_mem_m_app_share','share_type_code');
  end if;

  if coalesce(NEW.share_stock::text, '') = '' then
    NEW.share_stock := 0;
  end if;

  if coalesce(NEW.share_monthly::text, '') = '' then
    NEW.share_monthly := 0;
  end if;

  if coalesce(NEW.last_period::text, '') = '' then
    NEW.last_period := 0;
  end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tbiu_sc_mem_m_app_share() FROM PUBLIC;

CREATE TRIGGER tbiu_sc_mem_m_app_share
	BEFORE INSERT OR UPDATE ON sc_mem_m_app_share FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tbiu_sc_mem_m_app_share();


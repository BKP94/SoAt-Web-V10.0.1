-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;

DROP TRIGGER IF EXISTS tbiu_sc_mem_m_member_drop_loan ON sc_mem_m_member_drop_loan CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tbiu_sc_mem_m_member_drop_loan() RETURNS trigger AS $BODY$
BEGIN
  if coalesce(NEW.membership_no::text, '') = '' then
    CALL spa_dberrcol('sc_mem_m_member_drop_loan','membership_no',NEW.membership_no);
  end if;

  if coalesce(NEW.loan_type::text, '') = '' then
    CALL spa_dberrcol('sc_mem_m_member_drop_loan','loan_type',NEW.loan_type);
  end if;

  if coalesce(NEW.drop_reason::text, '') = '' then
    CALL spa_dberrcol('sc_mem_m_member_drop_loan','drop_reason',NEW.drop_reason);
  end if;

  NEW.entry_id := current_setting('app.login_id', true);
  NEW.entry_date := statement_timestamp();


RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tbiu_sc_mem_m_member_drop_loan() FROM PUBLIC;

CREATE TRIGGER tbiu_sc_mem_m_member_drop_loan
	BEFORE INSERT OR UPDATE ON sc_mem_m_member_drop_loan FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tbiu_sc_mem_m_member_drop_loan();


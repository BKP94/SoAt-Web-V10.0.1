-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;

DROP TRIGGER IF EXISTS tbiu_sc_mem_m_member_spouse ON sc_mem_m_member_spouse_info CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tbiu_sc_mem_m_member_spouse() RETURNS trigger AS $BODY$
BEGIN
  if coalesce(NEW.membership_no::text, '') = '' then
    CALL spa_dberrcol('sc_mem_m_member_spouse_info','membership_no',NEW.membership_no);
  end if;

  if coalesce(NEW.seq_no::text, '') = '' or NEW.seq_no = 0  then
    NEW.seq_no := 1;
  end if;

  if coalesce(NEW.salary_calloan::text, '') = '' then 
    NEW.salary_calloan := 0;
  else
    if coalesce(OLD.salary_calloan::text, '') = '' or OLD.salary_calloan <> NEW.salary_calloan then
      if NEW.salary_calloan < 0 then
        CALL spa_dberrcol('sc_mem_m_member_spouse_info','salary_calloan',NEW.salary_calloan);
      end if;
    end if;
  end if;

    if coalesce(NEW.level_code::text, '') = '' then NEW.level_code :=0;end if;
  --  if :new.employee_status is null then :new.employee_status:='0';end if;
  --  if :new.salary_rate_code is null then :new.salary_rate_code :=0;end if;
  if coalesce(NEW.salary_amount::text, '') = '' then
    NEW.salary_amount := 0;
  else
    if coalesce(OLD.salary_amount::text, '') = '' or OLD.salary_amount <> NEW.salary_amount then
      if NEW.salary_amount < 0 then
        CALL spa_dberrcol('sc_mem_m_member_spouse_info','salary_amount',NEW.salary_amount);
      end if;
    end if;
  end if;

  --  if :new.retire_status is null then :new.retire_status :=0;end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tbiu_sc_mem_m_member_spouse() FROM PUBLIC;

CREATE TRIGGER tbiu_sc_mem_m_member_spouse
	BEFORE INSERT OR UPDATE ON sc_mem_m_member_spouse_info FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tbiu_sc_mem_m_member_spouse();


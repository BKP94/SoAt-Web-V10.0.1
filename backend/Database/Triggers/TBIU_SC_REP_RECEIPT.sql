-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;

DROP TRIGGER IF EXISTS tbiu_sc_rep_receipt ON sc_rep_receipt CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tbiu_sc_rep_receipt() RETURNS trigger AS $BODY$
BEGIN

  if pka.auto_seqno then
     if coalesce(NEW.seq_no::text, '') = '' or NEW.seq_no = 0 then
        select max(seq_no) into STRICT NEW.seq_no
        from sc_rep_receipt
        where client_id = NEW.client_id;
        NEW.seq_no := coalesce(NEW.seq_no,0) + 1;
     end if;
  end if;

  if coalesce(NEW.SHARE_PERIOD::text, '') = '' then NEW.SHARE_PERIOD :=0;end if;
  if coalesce(NEW.SHARE_AMOUNT::text, '') = '' then NEW.SHARE_AMOUNT :=0;end if;
  if coalesce(NEW.SHARE_STOCK::text, '') = '' then NEW.SHARE_STOCK :=0;end if;
  if coalesce(NEW.EMER_PERIOD::text, '') = '' then NEW.EMER_PERIOD :=0;end if;
  if coalesce(NEW.EMER_PRINCIPAL::text, '') = '' then NEW.EMER_PRINCIPAL :=0;end if;
  if coalesce(NEW.EMER_INTEREST::text, '') = '' then NEW.EMER_INTEREST :=0;end if;
  if coalesce(NEW.EMER_BALANCE::text, '') = '' then NEW.EMER_BALANCE :=0;end if;
  if coalesce(NEW.NORM_PERIOD::text, '') = '' then NEW.NORM_PERIOD :=0;end if;
  if coalesce(NEW.NORM_PRINCIPAL::text, '') = '' then NEW.NORM_PRINCIPAL :=0;end if;
  if coalesce(NEW.NORM_INTEREST::text, '') = '' then NEW.NORM_INTEREST :=0;end if;
  if coalesce(NEW.NORM_BALANCE::text, '') = '' then NEW.NORM_BALANCE :=0;end if;
  if coalesce(NEW.SPEC_PERIOD::text, '') = '' then NEW.SPEC_PERIOD :=0;end if;
  if coalesce(NEW.SPEC_PRINCIPAL::text, '') = '' then NEW.SPEC_PRINCIPAL :=0;end if;
  if coalesce(NEW.SPEC_INTEREST::text, '') = '' then NEW.SPEC_INTEREST :=0;end if;
  if coalesce(NEW.SPEC_BALANCE::text, '') = '' then NEW.SPEC_BALANCE :=0;end if;
  if coalesce(NEW.ETC_AMOUNT::text, '') = '' then NEW.ETC_AMOUNT :=0;end if;
  if coalesce(NEW.INSURANCE_AMOUNT::text, '') = '' then NEW.INSURANCE_AMOUNT :=0;end if;
  if coalesce(NEW.MONEY_RETURN::text, '') = '' then NEW.MONEY_RETURN :=0;end if;
  if coalesce(NEW.MONEY_INCOME::text, '') = '' then NEW.MONEY_INCOME :=0;end if;
  if coalesce(NEW.TOTAL_AMOUNT::text, '') = '' then NEW.TOTAL_AMOUNT :=0;end if;
  if coalesce(NEW.client_id::text, '') = '' then
    NEW.client_id := '000';
  end if;


RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tbiu_sc_rep_receipt() FROM PUBLIC;

CREATE TRIGGER tbiu_sc_rep_receipt
	BEFORE INSERT OR UPDATE ON sc_rep_receipt FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tbiu_sc_rep_receipt();


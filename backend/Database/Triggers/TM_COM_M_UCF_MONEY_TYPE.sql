-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;

DROP TRIGGER IF EXISTS tm_com_m_ucf_money_type ON sc_com_m_ucf_money_type CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tm_com_m_ucf_money_type() RETURNS trigger AS $BODY$
DECLARE v_seq bigint;
BEGIN
    IF (OLD.MONEY_TYPE_ID IS NOT NULL AND OLD.MONEY_TYPE_ID::text <> '') THEN
        SELECT coalesce(MAX(SM_SEQ),0)+1 INTO STRICT v_seq FROM SM_COM_M_UCF_MONEY_TYPE WHERE MONEY_TYPE_ID = OLD.MONEY_TYPE_ID;
        INSERT INTO SM_COM_M_UCF_MONEY_TYPE(MONEY_TYPE_ID, SM_SEQ) VALUES (OLD.MONEY_TYPE_ID, v_seq);
    END IF;
    IF (NEW.MONEY_TYPE_ID IS NOT NULL AND NEW.MONEY_TYPE_ID::text <> '') THEN
        SELECT coalesce(MAX(SM_SEQ),0)+1 INTO STRICT v_seq FROM SM_COM_M_UCF_MONEY_TYPE WHERE MONEY_TYPE_ID = NEW.MONEY_TYPE_ID;
        INSERT INTO SM_COM_M_UCF_MONEY_TYPE(MONEY_TYPE_ID, SM_SEQ) VALUES (NEW.MONEY_TYPE_ID, v_seq);
    END IF;
IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tm_com_m_ucf_money_type() FROM PUBLIC;

CREATE TRIGGER tm_com_m_ucf_money_type
	AFTER INSERT OR UPDATE OR DELETE ON sc_com_m_ucf_money_type FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tm_com_m_ucf_money_type();


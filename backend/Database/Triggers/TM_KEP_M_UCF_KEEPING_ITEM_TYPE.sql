-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=192.168.101.29;sid=pean;port=1521

SET client_encoding TO 'UTF8';

SET check_function_bodies = false;

DROP TRIGGER IF EXISTS tm_kep_m_ucf_keeping_item_type ON sc_kep_m_ucf_keeping_item_type CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tm_kep_m_ucf_keeping_item_type() RETURNS trigger AS $BODY$
DECLARE v_seq bigint;
BEGIN
    IF (OLD.keeping_type_code IS NOT NULL AND OLD.keeping_type_code::text <> '') THEN
        SELECT coalesce(MAX(SM_SEQ),0)+1 INTO STRICT v_seq FROM SM_KEP_M_UCF_KEEPING_ITEM_TYPE WHERE keeping_type_code = OLD.keeping_type_code;
        INSERT INTO SM_KEP_M_UCF_KEEPING_ITEM_TYPE(keeping_type_code, SM_SEQ) VALUES (OLD.keeping_type_code, v_seq);
    END IF;
    IF (NEW.keeping_type_code IS NOT NULL AND NEW.keeping_type_code::text <> '') THEN
        SELECT coalesce(MAX(SM_SEQ),0)+1 INTO STRICT v_seq FROM SM_KEP_M_UCF_KEEPING_ITEM_TYPE WHERE keeping_type_code = NEW.keeping_type_code;
        INSERT INTO SM_KEP_M_UCF_KEEPING_ITEM_TYPE(keeping_type_code, SM_SEQ) VALUES (NEW.keeping_type_code, v_seq);
    END IF;
IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tm_kep_m_ucf_keeping_item_type() FROM PUBLIC;

CREATE TRIGGER tm_kep_m_ucf_keeping_item_type
	AFTER INSERT OR UPDATE OR DELETE ON sc_kep_m_ucf_keeping_item_type FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tm_kep_m_ucf_keeping_item_type();


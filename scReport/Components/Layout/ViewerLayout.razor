@inherits LayoutComponentBase

@* ViewerLayout — เลย์เอาต์เปล่าเต็มจอสำหรับแท็บ preview รายงาน
   (ไม่มี sidebar/topbar — เลียน legacy repViewer/page.aspx ที่เป็นหน้าเดี่ยวเต็มจอ) *@
<div class="rv-shell">
    @Body
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>
